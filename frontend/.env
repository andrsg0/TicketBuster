# Environment variables for TicketBuster Frontend

# API Gateway URL (proxy configured in vite.config.js for /api)
VITE_API_URL=/api

# Notification Service URL (Socket.io on port 4000)
# When using port-forward: kubectl port-forward service/notification-service 4000:4000
VITE_NOTIFICATION_URL=http://localhost:4000

# Keycloak Configuration (for OAuth2/OIDC)
VITE_KEYCLOAK_URL=http://localhost:8080/realms/ticketbuster
VITE_KEYCLOAK_CLIENT_ID=ticketbuster-frontend

# Development Mode (enables mock user)
VITE_DEV_MODE=true
